# Workflow Feature Status

Honest accounting of what exists, how complete it is, and where the caveats
live. Statuses: **Complete** (tested, wired, usable), **Partial** (works but
with documented limits), **Planned** (designed, not built), **Disabled**
(built but deliberately off).

Legend: each entry links its implementation and notes anything a user would
trip over.

## Authority & Policy

| Feature | Status | Notes |
|---|---|---|
| Deterministic task graph (states, dependencies, evidence, mutation epochs) | Complete | `src/kernel/`; no LLM/IO/UI deps |
| Application authorization (capability withholding, workspace binding) | Complete | `src/application/workflow.ts` |
| Spawn capability (default-deny subagent gating) | Complete | `src/adapters/acp.ts`, `src/application/host.ts` — `spawn` severity tops the escalation table; host metadata cannot relax it; enforced only via probe evidence (`docs/HOST_ADAPTERS.md`) |
| Enforcement-altering config-option gating | Complete | `src/integrations/acp-session.ts` — bypass/auto-approve family denied client-side before any wire call; agent-originated `config_option_update` rejected from retained config with a visible status |
| Process containment (bubblewrap on Linux) | Partial | `src/containment/platform.ts` — **policy-only passthrough on non-Linux, with a visible warning and an explicit `policy-only` result marker**; full isolation remains Linux-only |
| Cross-platform graceful degradation | Complete | `selectContainment()` — no silent fallback; `ContainedProcessResult.enforcement` distinguishes `enforced` from `policy-only` at the type level |
| Persistence (versioned JSON store, exclusive lock) | Complete | restart recovery marks orphaned IN_PROGRESS as FAILED |

## The Hub (universal authority)

| Feature | Status | Notes |
|---|---|---|
| `workflow-hub` daemon (loopback authority, discovery file, atomic lock, single instance) | Complete | `src/integrations/workflow-hub.ts`, `src/cli/hub.ts` |
| Lead agent: stock-ACP OpenCode under whole-agent Bubblewrap (`WORKFLOW_ACP_AGENT` selects; vendored Cline = fallback) | Complete | pivot 2026-09-16 (`docs/ACP_DECISION.md`): `createConfiguredAcpRuntime` defaults to a contained `opencode acp --pure` launch with a hub-written per-runtime config (metered provider via the loopback proxy, `permission: {edit/bash/task: "ask"}` routed through `WorkflowApplication.authorize`); `WORKFLOW_ACP_AGENT=cline` selects the vendored patched fallback. Gated metered probe: contained key-free turn with proxy-recorded usage (`test/acp-opencode-metered-probe.test.ts`) |
| Hub protocol v1 (discovery, `/health`, `/before-tool`, `/bash`, `/run/*`) | Complete | `docs/HUB_PROTOCOL.md`; conformance: `test/hub-protocol.test.ts` |
| Per-surface workspace binding | Complete | surfaces declare workspace per request |
| Dedicated task + evidence per scheduled run | Complete | `/run/begin`, `/run/finish` |
| Review gate (5-axis rubric, cross-run adversarial reviews) | Complete | `/review/rubric`, `/run/review`; cross-run + ≥3-axis anti-rubber-stamp, kernel-enforced via `reviewer` evidence; ported from opencode-workflow-guard |
| Hub-owned reviewer auto-launch on run completion | Complete | `createRunRegistry` reviewer seam (`src/integrations/run-registry.ts`) + production wiring (`src/integrations/hub-run-gates.ts`, `src/cli/hub.ts`) — contained ACP reviewer, contained git-diff sourcing, fail-closed verdict parsing; **exercised live end-to-end 2026-09-16 on the pivoted runtime: the scheduled probe's turn produced a real diff, the hub-owned OpenCode reviewer reviewed it, approved with the 5-axis structured summary the anti-rubber-stamp gate requires, and the run verified with no blocking reason**; earlier fail-closed outcomes (denials surfacing as run-blocking reasons, never silent passes) are reproduced in the probe logs |
| Review coverage manifest (W039) | Complete | `src/review/manifest.ts` + `src/integrations/hub-reviewer.ts` — review scope is a deterministic control-plane fact derived from `git status --porcelain=v1 -z --untracked-files=all` (every changed/untracked file, unlike `git diff HEAD` which silently omits untracked), obligations from the explicit `REVIEW_OBLIGATION_RULES` table, sha256 manifest digest; the hub reviewer embeds the manifest in the rubric and an approval fails closed unless its final `[COVERAGE]` line covers every manifest entry (`changes_requested` is never coverage-gated); the `[COVERAGE]` line binds reviewer claims to scope, it cannot prove reading — direct verifier-token `/run/review` submissions remain honest-client discipline; tests: `test/review-manifest.test.ts` (real-repo behavioral: large diffs, untracked, security-sensitive), `test/hub-reviewer.test.ts` |
| Risk-aware review partitioning (W040) | Complete | `src/review/partition.ts` + partitioned flow in `src/integrations/hub-reviewer.ts` — deterministic component units (src layers, tests paired by basename stem, ≤12-file chunk splits, docs/scripts/root), per-risk focused rules (`REVIEW_FOCUS_RULES`: security/authority/persistence/ui/tests/docs — never every rule in one prompt), integration review exactly when >1 unit requiring `[COVERAGE]` of unit ids, `partitionPreservesManifest` invariant with tamper tests; multi-unit scope reviews with one fresh isolated reviewer session per unit and any unit rejection or incomplete `[COVERAGE]` fails the whole review closed; single-unit/no-status compositions keep the exact W039 single-session behavior; unit sessions cost linearly with components — the intended cost-for-isolation trade; tests: `test/review-partition.test.ts`, `test/hub-reviewer.test.ts` |
| Review provenance and replay (W041) | Complete | `src/review/provenance.ts` + `src/integrations/review-provenance-store.ts` + runner journaling in `src/integrations/hub-reviewer.ts` — every hub review decision is journaled (NDJSON, `~/.workflow/review-provenance.jsonl`, `WORKFLOW_HUB_PROVENANCE` overrides, fail-closed corrupt-journal loads, bounded trims) bound to its fingerprint: commit hash, prompt digest (REAL in production: `/run/begin` accepts an optional `taskPrompt` and the scheduler always declares its prompt, threaded registry→reviewer), diff digest (changed content, not just path shape), W039 manifest digest, W040 partition digest, and a rule-set digest over every rule table plus the axis/diff gate constants; integration records use a NUL-prefixed provenance marker no path-derived component id can collide with; approvals journal BEFORE recording (unprovenanced approvals never record); interrupted partitioned reviews resume only units whose NEWEST same-fingerprint record was an approval with complete single-unit coverage — newer rejections shield older approvals, run-level aggregates never satisfy unit resume, any fingerprint change (including a different ask) resumes nothing, foreign workspaces never replay here; full-replay approval (zero fresh sessions) is possible by design and always journaled as `resumed from provenance: N`; the journal is 0600 same-user hub state disciplining honest clients like the verifier token — mutations DURING a review remain the kernel epoch's job, untracked-file content is bound by path only, direct `/run/review` submissions stay unjournaled; tests: `test/review-provenance.test.ts`, `test/review-provenance-store.test.ts`, `test/hub-reviewer.test.ts` |
| Hub-run test evidence (`test:<workspace>`) | Complete | requirement declared only when a real `WORKFLOW_TEAM_TASK_VERIFY_COMMAND` is configured (never the `true` default); `begin()` stales the workspace test subject so runs cannot inherit a predecessor's green tests; **exercised live 2026-09-16 in the scheduled probe's closed chain: the run task requires both reviewer and test evidence, so after the reviewer's approval the verify flow ran the workspace's own test command (a local `node --test` smoke test), recorded the passing environment evidence, and the run verified** — the full gate order (reviewer → tests → VERIFIED) is unit-tested in `test/hub-runs.test.ts` and live-proven |
| Executable security assurance case (W042) | Complete | `docs/SECURITY_ASSURANCE.md` + `test/security-assurance.test.ts` (+ `test/hub-guardless-startup.test.ts`) — twelve surface sections binding every material security claim to its implementation boundary plus a machine-parseable citation (`test/<file>#"<exact title>"`, `gated[ENV]: ...` for live probes, `manual[npm run ...]` for host-gated commands); the checker parses all 130+ citations and fails when any cited test, title, gate, or script ceases to exist, requires a non-empty verification on every claim row (empty-cell evasion rejected), existence-checks implementation-boundary file references, and floors citation weight per section as well as in aggregate; the hub's guardless-startup refusal is genuinely test-verified (a guard that cannot start rejects the provider composition, failing the hub's top-level startup await); 19 known residual risks stay stated (C3 model-channel exfiltration, G7 compaction, same-user honest-client limits, transport residuals); `THREAT_MODEL.md` cross-references the map |
| Subagent conformance probe | Complete | `test/acp-cline-subagent-probe.test.ts` (env-gated) ran live 2026-09-16 on the vendored pinned entry — **Red verdict recorded in `docs/HOST_ADAPTERS.md`: `spawn_agent` ran but neither the spawn tool call nor its permission reached the hub, so Cline stays `advisory`-capped/spawn-denied for spawn-inclusive workflows and is not labeled `enforced`**; re-run on every pinned version bump |
| OpenCode conformance probes (subagent, MCP mount, resume, skills delivery) | Complete | `test/acp-opencode-subagent-probe.test.ts`, `test/acp-opencode-mcp-mount-probe.test.ts`, `test/acp-opencode-resume-probe.test.ts` (env-gated; `--pure` launches measure the stock surface without operator plugins) ran live 2026-09-16 on 1.18.31 — **spawn Green (the `task` tool call projected and its permission reached the hub, so OpenCode is `enforced`-eligible per `docs/HOST_ADAPTERS.md` when the hub pins the ask config); hub-written MCP config mounts on both surfaces (project `opencode.json` and `XDG_CONFIG_HOME` — F1/G3 unblocked on OpenCode); `session/load` restores model context across restart (exact keyword recalled)** — plus the skills-delivery probe `test/acp-opencode-skills-delivery-probe.test.ts` (gate `WORKFLOW_ACP_OPENCODE_SKILLS`; wired into the S12 gated-probe catalog for the F1 single-delivery-path check, **no live verdict recorded yet** — it skips without its gate); re-run whenever the ambient `opencode` version changes (recorded in each probe's `agentInfo` evidence) |
| systemd user unit | Complete | `packaging/workflow-hub.service`; Linux/systemd only |
| Surface coverage: TUI, headless, zen, connectors, cron, desktop (`cline-hub`) | Complete | all resolve via env or discovery file, fail closed |
| Hub-native scheduler | Complete | `src/integrations/hub-scheduler.ts` — Vixie-semantics cron table at `~/.workflow/scheduler.json` (`WORKFLOW_HUB_SCHEDULES` overrides); fires review-gated contained runs; a rejected finish gate leaves the run VERIFYING (never fabricated); real-agent scheduled turn verified live 2026-09-16 (probe pass with a real `WORKFLOW_TEAM_TASK_VERIFY_COMMAND` test gate) |
| Advisory guidance for hub-composed prompts | Complete | `WORKFLOW_ADVISORY_STYLE` / `WORKFLOW_ADVISORY_NOTES` (one note per line) prepend honestly-advisory preamble text to every scheduled-run prompt (`advisoryGuidanceFromEnv` → scheduler `promptGuidance`); unset means prompts reach the turn unchanged |
| Per-run budget enforcement | Complete | token/cost caps from metering-proxy metrics; a violating event cancels the turn and the run fails with the budget as its recorded blocking reason |
| Hub guard interception (G2; added 2026-09-16 re-baseline) | Complete | the vendored workflow-guard-mcp guards every hub surface: the hub intercepts destructive commands and protected paths (`src/integrations/mcp-toolbox-guard.ts`, `test/hub-guard-interception.test.ts`) and refuses to start guardless (`test/hub-guardless-startup.test.ts`) — verified claim-by-claim in `docs/SECURITY_ASSURANCE.md` S10 |
| Admin control plane: credential custody (added 2026-09-16 re-baseline) | Complete | brokered credentials travel as `secret://` references only (never values in the hub's config), materialize per allowed consumer+workspace via the Secret Service on stdin, and the admin listing never returns secret material (`src/integrations/credentials.ts`, `test/credentials.test.ts`, `test/secret-service.test.ts`) — verified claim-by-claim in `docs/SECURITY_ASSURANCE.md` S10 |
| Hub session resource hygiene (W044) | Complete | the launcher that auto-spawns a hub owns it: `resolveWorkflowHub` hands the spawned child to its caller (`onSpawned`) and only the spawner terminates it (`terminateOwnedHub`, SIGTERM → graceful close removes discovery + lock); reuse of a probed hub gets no kill handle — `src/cli/tui.tsx` and `src/cli/ink-tui.tsx` reap on every exit path (normal exit, process exit, SIGTERM, SIGHUP/terminal close); the hub closes its guard MCP child when startup fails (lock-loser previously leaked it); regression-proven at unit level (`test/hub-autohub-reap.test.ts`: pid dies, baseline restored) and PTY end-to-end (`test/tui-cli.test.ts`: real launch → PTY close → hub pid dead, discovery + lock removed) — the operator-flagged ~300MB-per-launch accumulation is impossible by construction |
| Per-turn + cumulative session usage (W044, G1 surfacing) | Complete | `src/ui/usage.ts` — cumulative tokens/cost from the metering proxy plus a per-turn delta computed at turn boundaries (`running` baselines, `completed` publishes; failed/cancelled turns publish nothing); the ACP TUI header renders `N tokens · $C · +P this turn · $c` and the universal TUI's acp driver exposes the same accessor; the web UI already rendered cumulative + context size; tests: `test/usage-tracker.test.ts`, `test/tui-tasklist.test.ts` (usage render). Open: the monitor's hub-side per-session aggregation (`docs/TUI_PARITY.md`) and any cross-session persistence |
| Interactive session budget enforcement (W045, G1) | Complete | `src/integrations/session-budget.ts` — per-axis caps from `WORKFLOW_SESSION_BUDGET_{INPUT,OUTPUT,TOTAL}_TOKENS` / `_COST_USD` (malformed values throw: a broken cap never degrades to unenforced); `composeSessionWithBudget` wires a guard into every configured ACP runtime that checks the proxy's recorded usage on every session event — the first crossing cancels the in-flight turn (session/cancel) and the sticky violation refuses every later prompt through `WorkflowCodingSession`'s new `refusalGate` (surfaced as a failed event, never silent); the TUI header appends `! budget exceeded: ...` and the hub records the active mechanism per runtime (web `/api/session` `budgetMechanism` + composition logs for scheduled-run and reviewer runtimes); violation semantics shared with the scheduled-run guard (`budgetViolation`), scheduled caps themselves unchanged; server-side backstop when no local caps: OpenRouter per-key credit limit on the proxy's upstream key, named in the mechanism record. Tests: `test/session-budget.test.ts` (9), web mechanism assertion, TUI crossed-budget render. Residual: event-driven checks (a quiet turn aborts at its next event; proxy `onUsage` mid-stream wiring is future hardening) |
| Per-prompt task decomposition (W046) | Complete | `src/application/task-commands.ts` — the deterministic task-command port (create/activate/complete/retry through application commands only; kernel keeps validating dependencies, cycles, readiness, and evidence; the TaskGraph is never exposed) + `activeTaskCorrelation`: the ACP driver's stamp widened to `TaskId \| (() => TaskId)` (hub runs/reviewer/web keep fixed ids), so interactive proposals re-correlate with the ACTIVE eligible task at request time — activating a decomposed task mid-session moves the authorization target, and with nothing IN_PROGRESS the correlation falls to the `no-active-task` sentinel that the application gate denies as UNKNOWN_TASK (fail-closed, never a hung wire request). Decomposition suggestions stay advisory (plan projection, zero canonical effect; pinned by test). Tests: `test/interactive-task-commands.test.ts` (8) incl. the three-prompt interactive correlation test, blocked-decomposition rejection, and evidence-driven unlock. Open: a TUI task-selection palette (the port is the seam; web already speaks `/api/tasks`) |
| G5 error surfacing (W047) | Complete | every failure carries an actionable cause: a fail-closed permission denial threads its wire cause into the failed turn (`fail-closed: no_reject_option`); an agent crash mid-turn fails the pending turn with the exit cause (never a hang); boot/composition failures print `failed to start the contained session: …` and exit 1 instead of a stack trace (both launchers); an operator-armed turn watchdog (`WORKFLOW_ACP_TURN_TIMEOUT_MS`, off by default, malformed throws) bounds the one failure that produced no event — a hung turn. Tests: `test/error-surfacing.test.ts` (crash/deny/hang/custom-request fixtures + TUI renders) |
| G7 context visibility (W047) | Complete | agent-emitted usage/context signals project into the session record as advisory `agent-context` events (new event type; TUI renders dim `[context]` rows): standard kinds the projection doesn't specialize (`usage_update`) AND agent-custom notification methods — goose's `_goose/unstable/session/update` channel is tolerated and its nested payload projected; agent-custom REQUESTS stay fail-closed. G7 mitigation stated coherently: compaction-time CONTROL is explicitly out of hub scope over ACP; the mitigation is restart discipline, resume-backed (session-id projection + `WORKFLOW_ACP_RESUME` + probe-proven `session/load`) — visibility, never control. Open: agent-context rows not yet in the web transcript |
| goose third agent kind (W048) | Qualified (live 2026-09-17, 1.50.1) — `enforced` for the proven launch mode; azure metered pend + subagent-internal hooks resolved NEGATIVE on goose | `WORKFLOW_ACP_AGENT=goose` selects the contained `goose acp` profile (`src/integrations/goose-agent-config.ts`): approve mode (mutating tools cross `session/request_permission`; guard + bwrap remain the hard backstops), telemetry off, per-runtime `GOOSE_PATH_ROOT` hub-written config (the documented map-shaped extension schema — MOUNT probe green: skills-mcp stdio mounts, `list_skills` verbatim), provider fork (OpenRouter through the loopback metering proxy — OPENROUTER_HOST is the provider ROOT, live-evidenced; azure_foundry direct with hub-injected key env, fail-closed on missing pieces), model composed fail-closed (WORKFLOW_GOOSE_MODEL; openrouter defaults to openrouter/auto). Live probe verdicts in `docs/HOST_ADAPTERS.md`: PERMISSION Green (every mutating call asked with full allow/reject options, hub denials honored — pre-mutation interception proven → goose is `enforced` for this launch mode), SUBAGENT Green-denied (`delegate` projects AND asks — spawn-classified in KNOWN_SPAWN_TOOLS on this evidence), MOUNT/RESUME Green (keyword recalled across a contained restart), METERED Green-openrouter (placeholder-only credential; 2 usage_update channels project through W047's tolerance), HOOKS Green (documented plugin.json + hooks/hooks.json deny with on_failure:block stops a mutation under containment — the Cline-seam input resolves positive on the deny path), SUBAGENT-HOOKS NEGATIVE (granted-spawn, live twice: the delegated subagent's file-write fired no PreToolUse record and projected no ACP tool_call update — only top-level calls and the delegate spawn itself intercept). Pending honestly: the azure_foundry METERED run (no credentials in the probe env) and the Cline-side subagent-internal hooks arm (the operator's accepted-risk record or a live Cline-side proof decides the seam). Re-run the family on every goose version bump (weekly cadence) |

## Surfaces & UX

| Feature | Status | Notes |
|---|---|---|
| `workflow` (patched Cline TUI launcher) | Complete | resolves the hub; refuses to start without it |
| `workflow-monitor` (Ink monitoring TUI) | Complete | task panel, activity panel, log-enriched transcript; `npm run tui:workflow` |
| Pedagogical modes (5 modes, checkpoints, learner profile, PRIMM) | Complete | `src/pedagogy/`; profile at `~/.local/share/workflow/learner-profile.json` |
| Learner-level skill gating (F2 surface wiring) | Complete | the TUI mode bar binds the mode's required-skill set (`levels.json` shared with skills-mcp, `SKILLS_MCP_DIR` override) to the active task's mutation precondition; switching modes re-binds, no-required-skill modes clear it, malformed maps refuse startup (`applySkillGating` in `src/pedagogy/skill-gating.ts`) |
| Response/build styles (caveman speech, ponytail YAGNI build) | Complete | **model-advisory** — the model can ignore them; live switch through the `/` or Ctrl+P options menu; summaries kept by design |
| Style-savings measurement (`npm run style:eval`) | Complete | `src/cli/style-eval.ts` — per-style output deltas from cumulative driver usage |
| Browser projection | Partial | `src/ui/web.ts` demo; does not have the monitoring panels |

## MCP Toolbox (14 servers)

| Feature | Status | Notes |
|---|---|---|
| Log + progress emission on every server | Complete | leveled `notifications/message` on every server; `notifications/progress` with progressToken on all except `workflow-fs-exec-mcp` (logging-only — it emits leveled log messages per tool but no progressToken progress) |
| Learner profile as subscribable resource | Complete | learning-mcp only: `workflow://learner-profile` |
| Notification forwarding into agent UI | Complete | vendored patch: MCP notifications → tool-update surface |
| Lazy tool discovery (`CLINE_LAZY_MCP_TOOLS=1`) | Complete | discover/call meta-tools; **limit:** hashed tool names (>64 chars) don't unwrap for subject extraction but still gate as mutation |
| MCP result truncation (48k middle-cut) | Complete | `src/extensions/mcp/manager.ts` in the patch |
| Self-service MCP persistence | Complete | `~/.workflow/cline_mcp_settings.json` |

## Context & Memory

| Feature | Status | Notes |
|---|---|---|
| Compaction ↔ memory bridge (recall at start, flush on completion) | Complete | bounded (2k) provenance-tagged recall; outcome `lesson` records |
| Streamed logs reach the LLM | Never | logs/progress are UI-only by design |

## Honest Gaps

| Feature | Status | Notes |
|---|---|---|
| ~~Review-gate polish: P2/P3 findings → follow-ups ledger, monitor shows verdicts~~ | **Complete (superseded 2026-09-16; previously tracked as Planned in this section)** | shipped with plan Task A3: the monitor loads open P2/P3 review follow-ups from review-accountability-mcp into the Activity panel (`src/cli/ink-tui.tsx`, `src/ui/tui.tsx`) and surfaces run-gate verdicts, blocking reasons, and unverified completion claims from the hub `/snapshot` (`src/cli/hub-snapshot.ts`, `src/integrations/cline-tui-bridge.ts` gateObservability; `test/tui-tasklist.test.ts`, `test/review-followups.test.ts`) — kept here, marked superseded, per the never-delete re-baseline rule |
| Cross-platform isolation (macOS Seatbelt, Windows) | Planned | policy-only passthrough ships now; native isolation tiers remain open |
| Web-UI monitoring panels | Planned | port of the Ink panels |
