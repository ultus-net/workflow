/**
 * @deprecated The host adapter contract vocabulary lives in
 * `src/application/host.ts`; this shim re-exports it for compatibility with
 * existing adapter/CLI import paths.
 */
export * from "../application/host.js";
